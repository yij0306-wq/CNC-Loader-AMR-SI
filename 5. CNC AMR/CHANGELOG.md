# Dia Cut LOADER 13대 V40_CorrSpeed 업데이트 내역

## 주요 변경 사항 (V40_CorrSpeed)

1. **중앙통로 속도 제어 (Corridor Speed Control)**
   - AMR 대수에 따른 중앙통로 속도 제한 기능 추가
   - 중앙통로 진입 시 1배속으로 제한할 AMR 수 설정 컨트롤 추가 (`input-corr-amr-count`)
   - 상태 뱃지 UI 추가 (OFF / ON 상태 표시)

2. **조업 목표 및 생산량 관리**
   - 조업 목표 시간 설정 기능 추가 (8시간, 10시간, 12시간 등)
   - 생산 현황 UI 데이터 연동 (M3 2ND 등)

3. **배출 및 호출 로직 개선**
   - 배출 목표 단수 설정 (지정 단수 1칸 전 자동 호출 기능)
   - 사전 배출 (Pre-eject) 옵션 체크박스 추가

4. **모니터링 및 로깅**
   - 대기 지연 상세 로그 모달 창 추가 (`#logModal`)
   - AMR 대기 및 정체 상황에 대한 로그 확인 기능 추가

5. **레이아웃 및 UI 업데이트**
   - 13대 로더 대응을 위해 시뮬레이션 캔버스 너비 확장 (`2000px` -> `2800px`)
   - 데이터 초기화 및 시스템 초기화 버튼 스타일 변경
   - 배속 컨트롤 확장 (최대 500x 배속 기능 추가)
   - 회피 모드 및 감지 거리(Detect Range) 미세 조정 UI 추가
